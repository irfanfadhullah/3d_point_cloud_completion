_base_: default.yaml

model:
  name: "PMPNetWrapper"
  config: {}

output:
  out_path: "results/pmpnet"


# Mixed Precision Compatibility
amp_fp16: false  # CUDA extensions do not support half precision; use fp32
