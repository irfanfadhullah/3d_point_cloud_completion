_base_: default.yaml

model:
  name: MPGLNet
  config:
    num_pred: 16384

dataset:
  name: PCN

train:
  epochs: 400
  lr: 0.0001
  batch_size: 24
  scheduler: step
  step_size: 50
  gamma: 0.7

output_path: results/mpglnet
